language: node_js

node_js:
  - 'node'
  - '10'
  - '8'

branches:
  only:
  - master

install:
  - npm uninstall typescript --no-save
  - npm uninstall tslint --no-save
  - npm install

cache:
  directories:
    - node_modules

git:
  depth: 1
